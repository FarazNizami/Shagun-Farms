{
  "name": "shagun-farms-site",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "node ./build/build.js",
    "watch": "node ./build/build.js --watch"
  },
  "devDependencies": {
    "esbuild": "^0.19.0"
  }
}
